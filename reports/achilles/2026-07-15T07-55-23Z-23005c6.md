# SBOM Summary Report

- **Repository:** Sharnlyra/achilles
- **Ref:** main
- **Commit:** 23005c619f17e0216591a6e8837bf6a7ca33ffbe
- **Generated:** 2026-07-15 07:55:21 UTC

---

## Executive Summary
This scan of the `Sharnlyra/achilles` repository (main branch, commit `23005c6`) examined 2 files but found no matches to known open-source components, licenses, or dependencies. In practical terms, the scan did not identify any third-party code, license obligations, or vulnerabilities to report — either because the repository contains no recognizable open-source snippets/components, or because the scanned files are original/proprietary code. No vulnerability data was collected as part of this run.

## Risk Highlights
- **Copyleft licenses:** None flagged — `flagged_copyleft_components` is empty.
- **Vulnerabilities:** None detected, but this is because vulnerability scanning was not performed in this run (see Vulnerabilities section below), not because components were confirmed clean.
- **Unknown-risk licenses:** None present — `risk_counts.unknown` is empty/zero.
- Overall, there is nothing in this dataset requiring immediate human review.

## Scan Coverage
- Total files scanned: 2
- Matched files: 0
- Unmatched files: 2
- Unique components identified: 0
- Unique dependency components identified: 0
- Snippet matches found: 0

## License Breakdown
No license data was identified — `license_breakdown` is empty. This is consistent with zero matched components in this scan.

## Third-Party Components
No third-party components were detected (`components` list is empty). `components_truncated` is false, confirming this is a complete result, not a capped/partial list.

## Dependencies
No dependency components were detected (`dependency_components` list is empty). This section would normally list resolved package-manager dependencies, but none were found or declared in the scanned files.

## Vulnerabilities
Vulnerability scanning was **not part of this run** (`vulnerability_data_present: false`). This means the absence of vulnerability entries should **not** be interpreted as "no vulnerabilities found" — it simply means this capability was not active for this scan. To collect vulnerability data, enable a Dependency Track integration or use a premium API tier with SCANOSS.

## Snippet Matches
No snippet-level matches were found (`snippet_matches` is empty, `snippet_matches_total: 0`, `snippet_matches_truncated: false`). This indicates none of the scanned file content matched known open-source code snippets in the SCANOSS knowledgebase.

## Recommendations
1. **Verify scan scope:** Only 2 files were scanned and none matched — confirm this reflects the intended repository scope (e.g., check if the scan target correctly includes source directories, or if it was limited to non-code files like README/config).
2. **Re-run with broader coverage:** If the repository is expected to contain open-source dependencies (e.g., via package manifests like `package.json`, `requirements.txt`, etc.), ensure those files are included in the scan input.
3. **Enable vulnerability scanning:** To get meaningful security insights in future scans, integrate SCANOSS with Dependency Track or upgrade to a tier that supports vulnerability data collection.
4. **Re-scan after development progresses:** As the codebase grows, periodically re-run SCA/SBOM scans to catch newly introduced third-party components, licenses, and vulnerabilities early.
5. **No immediate license or security action needed** based on this scan's results, but treat this as an inconclusive baseline rather than a clean bill of health, given the very limited file coverage.
